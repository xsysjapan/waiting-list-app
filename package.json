{
  "name": "wait-list-app",
  "version": "0.0.1",
  "description": "wait-list-app.",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:backoffice": "cd backoffice && npm install",
    "install:all": "npm run install:backend && npm run install:frontend && npm run install:backoffice",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:backoffice": "cd backoffice && npm run build",
    "build:all": "npm run build:backend && npm run build:frontend && npm run build:backoffice",
    "copy:backend": "npx cpx -C backend/build/** build",
    "copy:frontend": "npx cpx -C frontend/build/** build/public",
    "copy:backoffice": "npx cpx -C backoffice/build/** build/public/backoffice",
    "copy:all": "npm run copy:backend && npm run copy:frontend && npm run copy:backoffice",
    "build": "npm run install:all && npm run build:all && npm run copy:all",
    "start": "cd build && npm install && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
